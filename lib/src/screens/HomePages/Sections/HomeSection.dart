import 'package:flutter/material.dart';

class Homesection extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        Container(
          child: const  Text(
              'Welcome My name is Jeyavarshan /n  I am a Flutter Developer'),
        )
      ],
    );
  }
}
